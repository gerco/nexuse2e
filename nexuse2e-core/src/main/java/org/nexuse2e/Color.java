package org.nexuse2e;

/**
 * @author gesch
 * a bunch of colors used for rendering the instance status in the web ui. Can be extended if required, 
 * keep the clustering implementation in mind before making global changes.
 */
public enum Color {
	GREY, 
	GREEN, 
	RED,
	YELLOW;
}
